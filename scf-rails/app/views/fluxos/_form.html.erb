<%= form_for(fluxo) do |f| %>
  <% if fluxo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fluxo.errors.count, "error") %> prohibited this fluxo from being saved:</h2>

      <ul>
      <% fluxo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
